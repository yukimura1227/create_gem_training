require "spec_helper"

describe CreateGemTraining do
  it "has a version number" do
    expect(CreateGemTraining::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
